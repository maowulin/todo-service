{
  "name": "todo-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "generate": "$HOME/go/bin/buf generate && node ./scripts/clean-generated.mjs",
    "dev": "go run main.go",
    "test": "go test ./..."
  }
}