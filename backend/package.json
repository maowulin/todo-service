{
  "name": "todo-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "generate": "$HOME/go/bin/buf generate && node ./scripts/clean-generated.mjs",
    "dev": "go run main.go",
    "test": "go test -race -count=1 -covermode=atomic -coverprofile=coverage.out .",
    "build": "pnpm run generate && go build -o server"
  }
}
